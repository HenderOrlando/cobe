{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Empresa list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Fechanace</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('usuarios_empresa_show', { 'id': entity.id }) }}">{{ entity.fechaNace }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('usuarios_empresa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('usuarios_empresa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('usuarios_empresa_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
