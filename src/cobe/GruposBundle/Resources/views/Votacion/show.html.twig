{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Votacion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Opciones</th>
                <td>{{ entity.opciones }}</td>
            </tr>
            <tr>
                <th>Fechafin</th>
                <td>{{ entity.fechaFin|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Opcionseleccionada</th>
                <td>{{ entity.opcionSeleccionada }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('grupos_votacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('grupos_votacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
