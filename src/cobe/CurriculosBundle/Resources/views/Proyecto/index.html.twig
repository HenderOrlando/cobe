{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Proyecto list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Fechainicio</th>
                <th>Fechafin</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('curriculos_proyecto_show', { 'id': entity.id }) }}">{{ entity.fechaInicio }}</a></td>
                <td>{% if entity.fechaFin %}{{ entity.fechaFin|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('curriculos_proyecto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('curriculos_proyecto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('curriculos_proyecto_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
