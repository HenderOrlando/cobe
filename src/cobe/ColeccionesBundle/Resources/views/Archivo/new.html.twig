{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Archivo creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('colecciones_archivo') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
